const http=require('http');const fs=require('fs');const path=require('path');const {WebSocket,WebSocketServer}=require('ws');const root=__dirname;const rooms=new Map();const mime={'.html':'text/html; charset=utf-8','.css':'text/css; charset=utf-8','.js':'text/javascript; charset=utf-8','.png':'image/png','.svg':'image/svg+xml'};
const server=http.createServer((request,response)=>{const requested=decodeURIComponent(request.url.split('?')[0]);const filePath=path.normalize(path.join(root,requested==='/'?'index.html':requested));if(!filePath.startsWith(root)||!fs.existsSync(filePath)||fs.statSync(filePath).isDirectory()){response.writeHead(404);return response.end('Not found')}response.writeHead(200,{'Content-Type':mime[path.extname(filePath)]||'application/octet-stream'});fs.createReadStream(filePath).pipe(response)});const wss=new WebSocketServer({server});const send=(client,message)=>client.readyState===WebSocket.OPEN&&client.send(JSON.stringify(message));const roster=room=>[...room.clients].map(client=>({id:client.clientId,name:client.displayName}));
wss.on('connection',socket=>{socket.on('message',raw=>{let message;try{message=JSON.parse(raw)}catch{return}if(message.type==='join'){if(!/^[A-Z0-9]{4}-[A-Z0-9]{4}$/.test(message.room))return socket.close();let room=rooms.get(message.room);if(!room){room={clients:new Set()};rooms.set(message.room,room)}socket.clientId=Math.random().toString(36).slice(2,10);socket.roomId=message.room;socket.displayName=String(message.name||'Visitante').slice(0,24);room.clients.add(socket);send(socket,{type:'welcome',id:socket.clientId,members:roster(room)});room.clients.forEach(client=>{if(client!==socket)send(client,{type:'user-joined',id:socket.clientId,name:socket.displayName,members:roster(room)})});return}if(!socket.roomId)return;const room=rooms.get(socket.roomId);if(message.to){const target=[...room.clients].find(client=>client.clientId===message.to);if(target)send(target,{...message,from:socket.clientId})}else room.clients.forEach(client=>{if(client!==socket)send(client,{...message,from:socket.clientId})})});socket.on('close',()=>{if(!socket.roomId)return;const room=rooms.get(socket.roomId);if(!room)return;room.clients.delete(socket);room.clients.forEach(client=>send(client,{type:'user-left',id:socket.clientId,members:roster(room)}));if(!room.clients.size)rooms.delete(socket.roomId)})});const port=Number(process.env.PORT)||3000;server.listen(port,()=>console.log(`America Roleplay running at http://localhost:${port}`));
